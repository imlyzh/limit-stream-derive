use limit_stream_derive::RmpSer;

#[derive(RmpSer)]
struct A { user: u32, name: String, ads: usize, adc: char }

#[test]
fn test() {
  // let src: TokenStream = "".parse().unwrap();
  // derive_ser(src);
}